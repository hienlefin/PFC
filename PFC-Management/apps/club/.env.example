# Copy to .env.local (same folder) then restart the app.
# Production: set a long random value; never commit it.
CLUB_SESSION_SECRET=pfc-club-dev-hmac-secret-do-not-use-in-production
CLUB_SESSION_TTL_SECONDS=604800
# CLUB_DB_PATH=
