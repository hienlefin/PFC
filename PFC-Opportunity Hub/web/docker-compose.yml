services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pfc
      POSTGRES_PASSWORD: pfc
      POSTGRES_DB: opportunity
    ports:
      - "5432:5432"
    volumes:
      - pfc_opp_pg:/var/lib/postgresql/data

volumes:
  pfc_opp_pg:
